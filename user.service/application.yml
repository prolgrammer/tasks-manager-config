server:
  port: 8080
spring:
  application:
    name: user.service
  datasource:
    url: jdbc:postgresql://postgresdb:5432/user_service
    username: postgres
    password: 1234
  kafka:
    consumer:
      group-id: user.service
